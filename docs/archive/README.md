# Archived design notes

Documents here have been superseded by the active design doc set but are retained for historical context.

| File | Superseded by | Why archived |
|---|---|---|
| `OPTIMIZATION.md` (+ `.zh-CN.md`) | `docs/CAPABILITY_GAPS.md`, `docs/MCP_SURFACE_DESIGN.md`, `docs/MCP_IMPLEMENTATION_TASKS.md` | Early brainstorm (O1–O10 candidate directions). Still-relevant directions have been absorbed into the active three-doc pipeline; the prioritization recommendation at the bottom of `OPTIMIZATION.md` is now in `MCP_IMPLEMENTATION_TASKS.md`. |

**These files are read-only.** Do not edit them. If a future change is needed, update the active docs instead.

The active doc set lives one level up in `docs/`:

- `CAPABILITY_GAPS.md` — what is missing relative to WPA / PerfView
- `MCP_SURFACE_DESIGN.md` — how to expose capabilities (Tool / Resource / Prompt)
- `MCP_IMPLEMENTATION_TASKS.md` — prioritization + concrete tasks (P0–P3)
